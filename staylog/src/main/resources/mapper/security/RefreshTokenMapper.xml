<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.staylog.global.security.mapper.RefreshTokenMapper">

    <resultMap id="RefreshTokenResultMap" type="com.staylog.global.security.entity.RefreshToken">
        <id property="id" column="ID"/>
        <result property="userId" column="USER_ID"/>
        <result property="token" column="TOKEN"/>
        <result property="expiresAt" column="EXPIRES_AT"/>
        <result property="createdAt" column="CREATED_AT"/>
        <result property="updatedAt" column="UPDATED_AT"/>
    </resultMap>

    <insert id="save" parameterType="com.staylog.global.security.entity.RefreshToken">
        INSERT INTO REFRESH_TOKENS (
            USER_ID,
            TOKEN,
            EXPIRES_AT,
            CREATED_AT,
            UPDATED_AT
        ) VALUES (
            #{userId},
            #{token},
            #{expiresAt},
            SYSDATE,
            SYSDATE
        )
    </insert>

    <select id="findByToken" resultMap="RefreshTokenResultMap">
        SELECT
            ID,
            USER_ID,
            TOKEN,
            EXPIRES_AT,
            CREATED_AT,
            UPDATED_AT
        FROM REFRESH_TOKENS
        WHERE TOKEN = #{token}
    </select>

    <select id="findByUserId" resultMap="RefreshTokenResultMap">
        SELECT
            ID,
            USER_ID,
            TOKEN,
            EXPIRES_AT,
            CREATED_AT,
            UPDATED_AT
        FROM REFRESH_TOKENS
        WHERE USER_ID = #{userId}
        ORDER BY CREATED_AT DESC
        FETCH FIRST 1 ROWS ONLY
    </select>

    <update id="updateToken" parameterType="com.staylog.global.security.entity.RefreshToken">
        UPDATE REFRESH_TOKENS
        SET
            TOKEN = #{token},
            EXPIRES_AT = #{expiresAt},
            UPDATED_AT = SYSDATE
        WHERE USER_ID = #{userId}
    </update>

    <delete id="deleteByToken">
        DELETE FROM REFRESH_TOKENS
        WHERE TOKEN = #{token}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM REFRESH_TOKENS
        WHERE USER_ID = #{userId}
    </delete>

    <delete id="deleteExpiredTokens">
        DELETE FROM REFRESH_TOKENS
        WHERE EXPIRES_AT &lt; SYSDATE
    </delete>

</mapper>
